name: cloud_storage_service_client
description: A new Flutter application.

version: 1.0.1+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^0.12.0
  cupertino_icons: ^0.1.2
  flutter_date_pickers: ^0.1.4
  file_picker: any
  ext_storage: any
  permission_handler: any
  logger: 1.0.0
  json_annotation: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any
  json_serializable: any

flutter:
  uses-material-design: true